# 航空座舱模拟系统答辩大纲

## 一、开场定位

本项目是一个基于 C 语言和 SDL2 开发的航空座舱模拟显示系统。系统面向航空驾驶舱与客舱信息展示场景，主要实现驾驶舱综合仪表显示、飞行管理计算机、发动机与告警显示、导航显示，以及客舱航班信息与地图展示。

答辩时可以用一句话概括：

> 本项目通过 SDL2 图形渲染和统一飞行数据快照，将 PFD、ND、EICAS、FMC 和客舱显示模块整合成一个可交互、可演示的航空模拟显示系统。

## 二、项目背景与意义

1. 航空座舱显示系统是飞行员获取飞行状态、航路、发动机和告警信息的重要界面。
2. 真实航空系统结构复杂，适合作为图形渲染、数据同步、模块化设计和交互控制的综合课程设计题目。
3. 本项目不追求真实飞行控制，而是聚焦于“数据驱动的仪表显示仿真”和“多模块协同展示”。

可以强调的意义：

- 训练 C 语言工程化组织能力。
- 熟悉 SDL2 图形、字体、图片、事件循环等基础能力。
- 通过统一数据中心解决多个仪表之间的数据同步问题。
- 将飞行仪表、航路、告警、客舱信息等内容组合成完整展示系统。

## 三、系统总体功能

项目主要包含两个入口方向：

1. 驾驶舱显示
   - PFD：主飞行显示，展示姿态、空速、高度、垂直速度、航向、自动驾驶目标等。
   - ND：导航显示，展示飞机位置、航向、航迹、地速、真空速、导航点和航路信息。
   - EICAS：发动机指示与机组告警系统，展示发动机 N1/N2、EGT、燃油、滑油、液压、电气、客舱压力等。
   - FMC：飞行管理计算机，支持航路页、起降页、爬升、巡航、下降、航段和状态页等界面。
   - 综合驾驶舱：将多个仪表渲染到统一驾驶舱背景中，支持缩放、拖拽、点击放大仪表和 FMC 交互。

2. 客舱信息显示
   - 展示航班号、出发地、目的地、当前位置、剩余时间等。
   - 展示北京到成都的模拟航线、飞机图标、飞行进度和飞行轨迹。
   - 支持天气信息更新和静态地图资源加载，失败时回退到本地资源。

## 四、系统架构思路

答辩时建议画成四层结构：

1. 启动层
   - `test.c` 中通过宏选择运行单独模块或启动器。
   - 启动器提供驾驶舱和客舱两个入口，便于演示。

2. 数据层
   - `SimDataCenter` 是核心数据中心。
   - 负责加载 `pfd.dat`、`nd.dat`、`eicas1.dat`、`eicas2.dat`、`KSEAKBFI.fms` 等资源。
   - 生成统一的 `SimSnapshot`，供 PFD、ND、EICAS 和告警模块使用。

3. 业务模块层
   - PFD、ND、EICAS、FMC、Cabin 各自维护自己的数据结构和更新逻辑。
   - 各模块既可以独立运行，也可以被综合驾驶舱复用。

4. 渲染与交互层
   - 使用 SDL2 创建窗口、渲染器、纹理和事件循环。
   - 每个仪表先渲染到独立纹理，再组合到综合驾驶舱场景。
   - 通过鼠标和键盘实现放大、拖拽、点击按钮、FMC 输入等交互。

推荐答辩表述：

> 系统采用“统一数据中心 + 独立显示模块 + 综合驾驶舱合成”的架构。这样做的好处是各仪表模块职责清晰，既能单独调试，又能在综合驾驶舱中共享同一份飞行状态，避免数据不同步。

## 五、核心数据流

可以按下面顺序讲：

1. 程序启动后初始化 SDL、TTF、IMG 等图形资源。
2. `SimDataCenter` 加载飞行数据文件和航路文件。
3. 数据中心根据当前仿真时间生成一帧 `SimSnapshot`。
4. PFD、ND、EICAS 从快照中提取各自需要的数据。
5. FMC 从航路数据中构建航路显示与页面状态。
6. 告警模块根据快照中的发动机、燃油、液压、电气等参数生成告警。
7. UI 模块将各自内容绘制到 SDL 纹理。
8. 综合驾驶舱把各纹理按布局贴到驾驶舱背景上，并处理用户交互。

一句话总结：

> 数据从文件和默认模拟数据进入统一数据中心，再被转换为一帧快照，最后由各显示模块消费并渲染到界面。

## 六、重点模块讲解

### 1. PFD 主飞行显示

讲述重点：

- 显示飞机姿态、滚转、俯仰、空速、高度、航向、垂直速度等核心飞行参数。
- 数据来自 `SimSnapshot`，如果统一数据不可用，则使用模块自身的模拟数据。
- 适合展示“数据驱动仪表动画”的效果。

答辩亮点：

> PFD 的关键是把连续变化的飞行参数映射成图形位置和刻度变化，例如姿态角影响人工地平线，空速和高度影响两侧刻度带。

### 2. ND 导航显示

讲述重点：

- 显示当前经纬度、航向、航迹、地速、真空速。
- 加载导航相关资源，如 `earth_fix.dat`、`earth_nav.dat`、`apt.dat`。
- 根据飞机当前位置重新计算附近导航点和显示状态。

答辩亮点：

> ND 模块体现了地理位置数据到屏幕坐标的转换，同时也展示了航路点、机场、导航台等信息的筛选和显示。

### 3. EICAS 发动机与告警显示

讲述重点：

- 上屏和下屏分别展示发动机、燃油、滑油、振动、液压、电气等系统参数。
- 数据结构统一为 `AircraftSystems_Data`。
- 告警信息来自 `SimSnapshot` 中的 warning 列表。

答辩亮点：

> EICAS 的价值在于把飞机系统状态集中展示，并将异常参数转化为告警，增强系统的完整性和真实感。

### 4. FMC 飞行管理计算机

讲述重点：

- 支持多个页面：INDEX、ROUTE、DEP/ARR、CLB、CRZ、DES、LEGS、STATUS。
- 支持键盘输入、鼠标点击、页面切换和航路执行。
- 使用机场、航点、航路文件等数据，为导航和航路显示提供支持。

答辩亮点：

> FMC 是项目中交互最强的模块，它不是静态显示，而是维护页面状态、输入缓存和航路选择逻辑。

### 5. 综合驾驶舱

讲述重点：

- 不是简单打开多个窗口，而是把 PFD、ND、EICAS、FMC 渲染到独立纹理后统一合成。
- 支持主视图、仪表放大视图、FMC 放大交互视图。
- 支持鼠标拖拽、滚轮缩放、点击模块进入局部放大、ESC 返回。

答辩亮点：

> 综合驾驶舱通过“离屏纹理渲染”复用各模块 UI，这样既保持模块独立，又能形成统一的驾驶舱展示效果。

### 6. 客舱信息显示

讲述重点：

- 面向乘客展示航班状态、当前位置、天气、地图、剩余时间。
- 飞行路线使用北京到成都的模拟航线。
- 地图和天气支持外部接口或缓存资源，失败时有本地 fallback。

答辩亮点：

> 客舱模块扩展了系统使用场景，让项目不仅包含飞行员视角，也包含乘客信息服务视角。

## 七、关键技术点

1. SDL2 图形渲染
   - 窗口创建、纹理绘制、字体渲染、图片加载、事件循环。

2. 离屏渲染与纹理合成
   - 各仪表先渲染到独立纹理，再贴到综合驾驶舱背景上。
   - 便于放大查看和统一布局。

3. 统一数据快照
   - `SimSnapshot` 集中保存飞行、导航、发动机、燃油、液压、告警等状态。
   - 多模块共享同一帧数据，保证显示同步。

4. 文件数据加载与降级策略
   - 优先加载 `assets` 中的真实或模拟数据。
   - 如果文件不可用，则使用 mock fallback，保证程序仍可运行。

5. 事件驱动交互
   - 鼠标点击、拖拽、滚轮缩放、键盘输入、FMC 页面切换。

6. 告警判定
   - 根据滑油压力、EGT、燃油、液压、电压、襟翼和起落架状态产生不同级别告警。

## 八、项目难点与解决方案

### 难点一：多仪表数据同步

问题：

- PFD、ND、EICAS、FMC 使用的数据类型不同，如果各自独立更新，容易出现时间和状态不一致。

解决：

- 设计 `SimDataCenter` 和 `SimSnapshot`。
- 每帧由数据中心统一更新时间，再分发给不同模块。

### 难点二：综合驾驶舱中复用各仪表 UI

问题：

- 如果把所有仪表绘制逻辑混在一个文件中，代码会非常复杂。

解决：

- 保留每个模块自己的 UI 渲染函数。
- 在综合驾驶舱中用目标纹理承接各模块输出，再统一合成。

### 难点三：航路、导航点和地图信息的组织

问题：

- 航空导航数据量大，包含机场、航点、导航台、航路等多种数据。

解决：

- 使用独立数据结构管理导航点、航路点和机场信息。
- ND 按当前位置重新计算可见导航点。
- FMC 解析航路文件，统一转为 planned route。

### 难点四：外部资源不稳定

问题：

- 图片、字体、天气、地图、飞行数据文件可能缺失或加载失败。

解决：

- 多字体路径回退。
- 图片加载失败时使用本地或绘制 fallback。
- 飞行数据不可用时使用 mock 数据，保证演示稳定。

## 九、演示流程建议

答辩演示可以控制在 3 到 5 分钟：

1. 打开启动器，说明系统有驾驶舱和客舱两个入口。
2. 进入驾驶舱，展示整体驾驶舱背景和多个仪表同时运行。
3. 点击 PFD 或 ND，展示放大查看效果。
4. 点击 FMC，展示页面切换、按钮交互或键盘输入。
5. 说明 EICAS 和告警模块如何根据数据变化显示系统状态。
6. 返回启动器，进入客舱显示。
7. 展示地图、飞机位置、天气、航班信息和飞行进度。

演示时推荐强调：

> 大家看到的多个仪表不是静态图片，而是由数据中心驱动、每帧更新的 SDL 图形界面。

## 十、PPT 结构建议

1. 标题页
   - 航空座舱模拟系统
   - 姓名、班级、指导老师、日期

2. 项目背景
   - 航空座舱显示系统的作用
   - 课程设计目标

3. 需求分析
   - 驾驶舱仪表显示
   - 飞行管理交互
   - 客舱信息显示
   - 数据同步与稳定运行

4. 总体架构
   - 启动层、数据层、模块层、渲染交互层
   - 建议放一张架构图

5. 数据流设计
   - assets 数据文件
   - SimDataCenter
   - SimSnapshot
   - PFD/ND/EICAS/FMC/Cabin

6. 主要功能展示
   - PFD
   - ND
   - EICAS
   - FMC
   - 综合驾驶舱
   - 客舱显示

7. 关键技术实现
   - SDL2 渲染
   - 离屏纹理合成
   - 文件加载
   - 事件处理
   - fallback 机制

8. 难点与解决方案
   - 多模块同步
   - 综合界面合成
   - 航路数据处理
   - 资源加载稳定性

9. 测试与运行效果
   - 单模块测试
   - 综合驾驶舱测试
   - 客舱显示测试
   - 异常资源回退测试

10. 总结与展望
   - 当前成果
   - 不足
   - 后续优化方向

## 十一、总结页可以这样讲

本项目完成了一个基于 C/SDL2 的航空座舱模拟显示系统，实现了驾驶舱和客舱两个展示场景。驾驶舱部分集成了 PFD、ND、EICAS 和 FMC 等核心模块，并通过统一数据中心保证多个仪表之间的数据同步。客舱部分实现了航班、地图、天气和飞行进度展示。项目中重点解决了多模块协作、图形渲染、数据加载、界面交互和异常资源回退等问题。

后续可以继续优化的方向包括：

- 接入更真实的飞行模拟器数据源，例如 X-Plane 实时数据。
- 增强 FMC 航路规划和性能计算能力。
- 增加更多异常场景和告警逻辑。
- 优化 UI 细节和跨平台构建方式。

## 十二、老师可能提问与回答思路

### 1. 为什么要设计 SimDataCenter？

因为多个仪表都依赖飞行状态数据。如果每个模块单独读取和更新时间，容易出现显示不同步。`SimDataCenter` 可以统一加载数据、统一推进仿真时间，并生成一帧 `SimSnapshot`，让 PFD、ND、EICAS 和告警模块消费同一份数据。

### 2. 项目中 SDL2 主要负责什么？

SDL2 负责窗口创建、图形绘制、纹理渲染、字体显示、图片加载和事件处理。项目中的仪表刻度、文字、背景图片、FMC 面板和鼠标键盘交互都基于 SDL2 实现。

### 3. 综合驾驶舱如何把多个仪表放到一个界面？

每个仪表模块先渲染到一个独立 SDL 目标纹理，然后综合驾驶舱按照布局坐标把这些纹理贴到驾驶舱背景图上。这种方式可以复用单独模块的渲染逻辑，也方便实现点击放大。

### 4. 如果数据文件缺失，程序会怎样？

项目设计了 fallback 机制。优先加载 assets 中的数据文件，如果加载失败，则使用模块内置 mock 数据或默认航路，保证程序仍然可以运行和演示。

### 5. FMC 和 ND 的区别是什么？

FMC 更偏向飞行计划管理和交互输入，例如航路页、起降页、航段页；ND 更偏向导航态势显示，例如当前位置、航向、航迹、导航点和航路在屏幕上的展示。

### 6. 项目最大的难点是什么？

最大难点是多模块协同。每个仪表都有自己的数据结构和渲染逻辑，但综合驾驶舱需要它们在同一时间轴下同步显示。解决方法是抽象统一数据快照，并通过目标纹理合成方式整合界面。

### 7. 这个项目有哪些不足？

目前系统主要是显示仿真，飞行动力学和真实飞控逻辑还比较简化；部分数据是文件或 mock 数据；FMC 的功能还没有达到真实飞机系统复杂度；跨平台构建也可以进一步完善。

### 8. 后续如何提升？

可以接入 X-Plane 或其他模拟器实时数据，完善真实航路计算和性能页面，增加更多故障告警场景，并将构建系统改为 CMake 以提高可维护性和跨平台能力。

## 十三、建议答辩主线

整场答辩不要按代码文件逐个讲，建议围绕一条主线：

> 为什么做这个项目 -> 系统要实现什么 -> 总体架构怎么设计 -> 数据如何流动 -> 各模块如何显示 -> 遇到什么难点 -> 怎么解决 -> 最终效果和不足。

最推荐的讲述顺序：

1. 先讲项目定位：航空座舱模拟显示系统。
2. 再讲总体架构：统一数据中心 + 独立模块 + 综合合成。
3. 然后讲核心功能：PFD、ND、EICAS、FMC、客舱。
4. 接着讲技术亮点：SDL2、纹理合成、数据快照、fallback。
5. 最后讲演示效果、问题和展望。
